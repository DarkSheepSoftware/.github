{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>DarkSheepSoftware/.github:renovate-config"
  ],
  "packageRules": [
    {
      "matchManagers": [ "npm" ],
      "matchUpdateTypes": [ "pin", "minor", "patch", "lockFileMaintenance" ],
      "automerge": true
    },
    {
      "matchManagers": [ "github-actions" ],
      "automerge": true
    }
  ]
}
